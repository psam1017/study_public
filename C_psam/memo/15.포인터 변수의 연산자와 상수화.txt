주소 연산자 : &var
-> var의 저장공간, 즉 주소

포인터 연산자 : int* ptr = &var
-> ptr에 var의 주소를 저장

참조 연산자 : *ptr
-> var 저장공간 안에 있는 값



const char* 포인터명 : 그 주소에 접근 후 그 안의 값을 변경할 수 없다.

const char* const 포인터명 : 주소값 자체를 변경할 수 없다.

*포인터에 접근하여 주소값을 수정하는 것은 바람직한 접근이 아니다.