GET

주소에 데이터를 추가하여 전달한다.
쿼리 문자열에 포함되어 전송되므로 길이에 제한이 있다.
주소에 데이터가 보이므로 보안상 취약할 수 있다.
POST보다 빠르게 전송이 가능하다.

POST

데이터를 별도로 첨부하여 전달한다.
브라우저 히스토리에 남지 않고, 쿼리와 별도로 전송되므로 길이 제한이 없다.
주소에 데이터가 보이지 않으므로 비교적 보안성이 좋다.
GET보다 느리게 전송된다.